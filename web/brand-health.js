(()=>{'use strict';const $=(s,r=document)=>r.querySelector(s);let sourceObserver=null;function sync(){const source=$('#serviceStatus'),badge=$('.ref-brand-status');if(!badge)return;const text=String(source?.textContent||'offline').trim().toLowerCase(),online=text==='online';const label=$('span',badge),dot=$('i',badge);if(label)label.textContent=online?'online':'offline';badge.dataset.state=online?'online':'offline';badge.setAttribute('aria-label',online?'Atlas online':'Atlas offline');badge.title=online?'Backend Atlas отвечает':'Backend Atlas сейчас недоступен';if(dot)dot.setAttribute('aria-hidden','true')}function bind(){const source=$('#serviceStatus');if(source&&!source.dataset.brandHealth){source.dataset.brandHealth='1';sourceObserver=new MutationObserver(sync);sourceObserver.observe(source,{childList:true,characterData:true,subtree:true})}sync()}new MutationObserver(bind).observe(document.documentElement,{childList:true,subtree:true});window.addEventListener('online',()=>setTimeout(sync,100));window.addEventListener('offline',sync);document.readyState==='loading'?document.addEventListener('DOMContentLoaded',bind,{once:true}):bind();window.AtlasBrandHealth={sync};})();